source 'https://rubygems.org'

# 使用 Jekyll 而不是 github-pages gem 以支持自定义插件
gem 'jekyll', '~> 3.10'
gem 'kramdown-parser-gfm'

# Ruby 3.4 所需的依赖
gem 'base64'
gem 'logger'
gem 'bigdecimal'
gem 'csv'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
end
